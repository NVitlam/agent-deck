# `fixtures/hook-events/` — redacted hook-event capture (CC 2.1.234)

`cc-2.1.234-redacted.jsonl` is the **liveness-side** fixture: real hook payloads received by a
loopback listener from the pinned Claude Code version, with all content values destroyed and all
*structure* preserved. It is the counterpart to `fixtures/cc-2.1.234/`, which pins the content side.

Generated by `redact-capture.mjs` in this directory from a raw capture that is **gitignored and must
never be committed** — raw payloads carry verbatim `tool_input`, `tool_response` and
`last_assistant_message` (one observed at 4,090 characters of assistant text). That is the exact leak
class the Phase 1 history scrub removed from this repo.

```
node fixtures/hook-events/redact-capture.mjs \
  --in spike/hook-events-r3.jsonl \
  --out fixtures/hook-events/cc-2.1.234-redacted.jsonl
```

## Capture conditions (R3/R2, 2026-08-19)

Listener bound **first** on `127.0.0.1:47821`, then load generated by this repo's own Phase 2 work:
three `phase-implementer` subagents running in parallel worktrees (G8 — the only project ever
observed is `agent-deck` itself). The capture snapshot was frozen before redaction; the live file
keeps growing while a listener is bound, so **never diff the fixture against the live capture** —
they are supposed to differ.

## What survives redaction, and why

**Key presence and absence is preserved exactly, per event.** This is the whole point of the fixture:
CC signals the main thread by **omitting `agent_id`**, not by sending `"main"` (0 occurrences of the
literal, here and in every earlier capture). A redactor that defaulted absent keys to `null` or `""`
would destroy the one property this fixture exists to pin. Verified after generation: 285/285 events
key-set-identical to the raw snapshot, 0 events with `agent_id` presence altered.

Kept verbatim: `hook_event_name`, `session_id`, `agent_id`, `agent_type`, `tool_use_id`, `tool_name`,
`prompt_id`, `transcript_path`, `agent_transcript_path`, `cwd`, `duration_ms`, `permission_mode`,
`stop_hook_active`, `source`, `matcher`. Paths and ids name only this repo, whose slug and session
uuids are already committed under `fixtures/cc-2.1.234/`.

Everything else becomes a shape descriptor — `[redacted:string:1234]`, `[redacted:object:2]`,
`[redacted:array:4]` — so tests can assert on shape and size without any content existing.

## Measured composition of this capture

| event | count | `agent_id` present | `tool_use_id` present |
|---|---|---|---|
| `PostToolUse` | 133 | 116/133 | 133/133 |
| `PreToolUse` | 147 | 129/147 | 147/147 |
| `Stop` | 1 | 0/1 | 0/1 |
| `SubagentStart` | 3 | 3/3 | 0/3 |
| `SubagentStop` | 1 | 1/1 | 0/1 |

**Do not assert these counts in tests.** They describe one capture and change on the next harvest;
counts hard-coded against a capture read as regressions later. Derive from the file.

## What this capture settled

- **`SubagentStart` is CONFIRMED on pinned CC 2.1.234** — 3/3 well-formed. Every prior document in
  this repo called it "never observed, treat as unconfirmed". That was an artifact of our own
  configuration: it had never been *registered* in the hook block, so it could not have fired. It was
  registered at `74c5236` and arrived immediately, without a CC restart — settings are re-read per
  hook invocation.
- **`SubagentStart` carries no `tool_use_id`** (0/3). It therefore cannot, by itself, tell you which
  parent `tool_use` block spawned the agent. That join still comes from the JSONL sidecar's
  `meta.toolUseId`. Liveness may not assume the hook tap can graft.
- **`SubagentStop` carries `agent_transcript_path`**, resolving to
  `projects/<slug>/<sessionId>/subagents/agent-<agentId>.jsonl`. The undocumented directory layout is
  corroborated from the *documented* hook side — a genuine reduction in the project's #1 risk
  (layout drift), and a direct file pointer for liveness.
- **The `Stop` / `SubagentStop` split reproduces**: `Stop` omits `agent_id`, `SubagentStop` carries
  it. Same 100%/0% split as the purged 181-event capture and the 246-event live log.
- **Undocumented keys observed**: `prompt_id` (all events), `effort`, `permission_mode`,
  `duration_ms` (PostToolUse), `background_tasks`, `session_crons`, `last_assistant_message`,
  `agent_transcript_path`. Consumers must preserve unknown keys rather than reject on them.
- **`SessionStart` is CONFIRMED on pinned CC 2.1.234**, pinned in `cc-2.1.234-sessionstart.jsonl`
  (2 events). It is absent from the main capture above, and that absence was never evidence about CC:
  it fires at session *onset*, and every earlier capture was taken by a listener that bound after the
  observing session had already started. Opening a fresh window with the listener up produced it
  immediately. Measured payload — a **smaller** key set than any other event:
  `session_id`, `transcript_path`, `cwd`, `hook_event_name`, `source`. **No `agent_id`** (consistent
  with the main-thread rule), **no `tool_use_id`**, and notably **no `prompt_id`**, which every other
  event carries. `source` is `"startup"` on both. Per spec §C4 liveness must still never *require* it.
