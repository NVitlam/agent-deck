div
  div [app] .app data-altitude="deck" data-degraded="false" data-liveness="unsupported" data-refused="true" data-view-mode="list"
    div .chrome
      button [view-toggle] .toggle aria-pressed="false" data-view-mode="list" type="button" "List"
    div .body
      nav [session-rail] .rail aria-label="Sessions"
        h2 .rail-title "Sessions"
        ul
          li
            button [rail-item] .rail-item .selected aria-current="true" data-liveness="unsupported" data-refused="true" data-selected="true" data-session-id="session-unsupported" type="button"
              span .rail-label "main session"
              span .rail-meta
                span [rail-liveness] .liveness .liveness-unsupported data-liveness="unsupported" title="the transcript layout was not recognised, so no tree is rendered for this session" "{}unsupported"
                  span .dot aria-hidden="true"
      main [main] .main
        section [refusal-screen] .refusal data-liveness="unsupported" role="alert"
          h2 "Unsupported session"
          p "Agent Deck did not recognise the transcript layout for {}, so it is not rendering a tree for it."
            code [refusal-session-id] "session-unsupported"
          p .why "This is deliberate. Agent Deck reads an undocumented on-disk format; when that format does not match what it was pinned against, a partly-understood tree would be a confident guess. It refuses instead."
          p [refusal-cause] .why "The usual cause is that Claude Code changed its on-disk format since this version of Agent Deck was pinned to it."
          p .why "Other sessions in the rail are unaffected, and nothing was changed on disk — Agent Deck only ever reads."
